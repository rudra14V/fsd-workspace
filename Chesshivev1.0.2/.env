# SMTP Configuration for Email Sending
# Replace with your actual SMTP server details

# SMTP Server Host (e.g., smtp.gmail.com, smtp.mailgun.org, etc.)
SMTP_HOST=smtp.gmail.com

# SMTP Port (usually 587 for TLS, 465 for SSL, 25 for plain)
SMTP_PORT=587

# Use secure connection (true for SSL/TLS, false for plain)
SMTP_SECURE=false

# Email address used for sending emails (your sender email)
SMTP_USER="imv25126636@gmail.com"

# Password or API key for the SMTP user
SMTP_PASS="vqch yqaw swjc uzxz"

# From email address (can be the same as SMTP_USER)
SMTP_FROM="imv25126636@gmail.com"

# Frontend URL for magic link (default is localhost:3001)
FRONTEND_URL=http://localhost:3001